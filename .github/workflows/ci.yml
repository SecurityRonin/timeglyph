name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: "0"

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      # Pin to the same toolchain as rust-toolchain.toml (timeglyph is an app:
      # declared MSRV == the pinned toolchain). Do not float `toolchain: stable`.
      - uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - run: cargo test --all-features
      - run: cargo clippy --all-targets --all-features -- -D warnings
      - run: cargo fmt --check

  # The lens (GUI overlay) is a separate crate, excluded from the root workspace,
  # so the `test` job never touches it. Build it natively on each shipped
  # platform to compile + LINK the platform picker CI otherwise never sees — UI
  # Automation on Windows (x86_64, the exact shipped target), the Accessibility
  # API on macOS, and AT-SPI on Linux.
  lens:
    name: Lens build (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - name: Install GUI build deps (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libxkbcommon-dev libgl1-mesa-dev libwayland-dev \
            libx11-dev libxcursor-dev libxrandr-dev libxi-dev libxcb1-dev pkg-config
      - uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - name: Build lens (compiles + links the platform picker)
        working-directory: lens
        run: cargo build --all-targets
      - name: Clippy lens
        working-directory: lens
        run: cargo clippy --all-targets -- -D warnings
      - name: Rustfmt lens
        working-directory: lens
        run: cargo fmt --check

  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: llvm-tools-preview
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - uses: taiki-e/install-action@a402910a723481c4c80d006d75298c796a9c8695 # cargo-llvm-cov
        with:
          tool: cargo-llvm-cov
      # All test targets, all features (integration tests carry the coverage, so
      # NOT --lib). The gate requires 100% LINE **and** function coverage of the
      # LIBRARY (ADR-0008), honoring `// cov:unreachable` markers on
      # provably-dead defensive arms (raw --fail-under-lines can't exempt
      # those); main.rs (the Humble CLI shell) is excluded by the script.
      # The JSON carries per-function counts, the LCOV per-line `DA:` hits — the
      # gate needs both, and exits 2 if either is missing.
      - name: Coverage + 100% line/function gate
        run: |
          cargo llvm-cov --all-features --json --output-path cov.json
          cargo llvm-cov report --lcov --output-path lcov.info
          python3 scripts/coverage-gate.py cov.json lcov.info
      - name: Upload to Codecov
        uses: codecov/codecov-action@0f8570b1a125f4937846a11fcfa3bcd548bd8c97 # v4.6.0
        with:
          files: lcov.info
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false

  secrets:
    name: Secret Scan (gitleaks)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          fetch-depth: 0
      # The gitleaks GitHub Action requires a paid license for org repos; the
      # gitleaks CLI is free. Install + run it directly (fleet pattern).
      - name: Install gitleaks
        # Pin the version rather than query the GitHub API for "latest": the
        # unauthenticated api.github.com call rate-limits on shared CI IPs, which
        # left VERSION empty and 404'd the download. Bump deliberately.
        run: |
          VERSION=8.30.1
          curl -sSfL "https://github.com/gitleaks/gitleaks/releases/download/v${VERSION}/gitleaks_${VERSION}_linux_x64.tar.gz" \
            | tar xz -C /tmp gitleaks
      - name: Run gitleaks
        run: /tmp/gitleaks detect --source . --config .gitleaks.toml

  deny:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
      - name: Install cargo-deny
        uses: taiki-e/install-action@a402910a723481c4c80d006d75298c796a9c8695
        with:
          tool: cargo-deny
      - name: Check advisories, licenses, bans, sources
        run: cargo deny check

  freshness:
    # Advisory dependency-freshness gate: fails if the committed Cargo.lock is
    # stale relative to the requirements (see CLAUDE.md "Dependency Freshness").
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
      - run: cargo update --locked

  lean-build:
    # Gate: `cargo add timeglyph --no-default-features` must NOT pull in clap,
    # csv, or tracing-subscriber.  Those are CLI-only deps; a library consumer
    # doing `default-features = false` should get a lean build with only the
    # core engine deps (jiff, thiserror, hex, serde, serde_json, tracing facade).
    # Enforced now that clap/csv/tracing-subscriber sit behind the `cli`/`csv`
    # features (a lean library build carries none of them).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: dtolnay/rust-toolchain@1.96.0
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - name: Lean lib build (no default features)
        run: cargo build --no-default-features
      - name: Assert clap / csv / tracing-subscriber absent from lean tree
        run: |
          cargo tree --no-default-features -e no-dev --prefix none > /tmp/lean-tree.txt
          UNWANTED=$(grep -E '^clap |^csv |^tracing-subscriber ' /tmp/lean-tree.txt || true)
          if [ -n "$UNWANTED" ]; then
            echo "FAIL: CLI-only deps present in lean (no-default-features) build:"
            echo "$UNWANTED"
            exit 1
          fi

  wasm-build:
    # Gate: the core engine must compile to wasm32 so the WASM playground and
    # browser / CyberChef embeddings stay buildable. Depends on the engine being
    # pure (no I/O, no clap/csv) and jiff + serde being wasm-compatible.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: dtolnay/rust-toolchain@1.96.0
        with:
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - name: WASM lib build (wasm32-unknown-unknown, no default features)
        run: cargo build --lib --no-default-features --target wasm32-unknown-unknown
      - name: WASM playground binding build (wasm32)
        run: cargo build --manifest-path wasm/Cargo.toml --target wasm32-unknown-unknown

  python-wheel:
    # Gate: the PyO3 Python bindings build a wheel and its tests pass. Publishing
    # to PyPI is a separate credentialed release step; this proves the wheel is
    # always buildable and correct (plaso/Timesketch/Jupyter consume it).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: dtolnay/rust-toolchain@1.96.0
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.11"
      - run: pip install maturin pytest
      # One v* tag ships the crate AND the wheel, so the wheel version must equal
      # the crate version. release-plz bumps ONLY the root crate on a release, so an
      # equality *check* here can never pass on a release PR (bindings/ would still
      # lag). Instead stamp the bindings from the crate — the single source of truth —
      # making drift impossible by construction (this closes the 0.3.0-vs-0.4.0 gap
      # without a guard release-plz can't satisfy). The same stamp runs in the wheel
      # build (release.yml) so published wheels are always at the crate version.
      - name: sync bindings version to the crate
        run: ./bindings/python/sync-version.sh
      - name: Build + test the Python wheel
        working-directory: bindings/python
        run: |
          maturin build --release
          pip install target/wheels/*.whl
          pytest tests/ -q

  vet:
    name: Cargo Vet (supply-chain)
    runs-on: ubuntu-latest
    # Complements `deny` (known-bad advisories/licenses) with the supply-chain-
    # injection layer: every dependency version must be human-source-reviewed or
    # covered by an imported aggregate audit set (Google/Mozilla/Bytecode-Alliance/
    # Embark). Config in supply-chain/{config,audits,imports}.toml.
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: dtolnay/rust-toolchain@stable
      - name: Install cargo-vet
        uses: taiki-e/install-action@59012be0884e296ca2da49b530610e72c49039ad # v2.81.6
        with:
          tool: cargo-vet
      - name: Fetch dependencies
        run: cargo fetch
      - name: Check supply chain
        run: cargo vet --locked
